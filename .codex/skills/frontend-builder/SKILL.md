# Frontend Builder Skill

Scope:
- React/Vite SOC dashboard UI
- API integration with backend endpoints
- Responsive layout and operational dashboard styling

Checklist:
1. Use real API data where available.
2. Keep loading/failure states explicit.
3. Keep controls and tables stable across mobile/desktop.
4. Run `npm run build` in `frontend/`.
