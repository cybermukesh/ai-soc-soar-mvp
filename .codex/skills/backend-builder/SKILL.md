# Backend Builder Skill

Scope:
- FastAPI routes
- SQLite/Postgres-ready persistence
- Wazuh/OpenSearch connectors
- LLM triage service behavior

Checklist:
1. Add typed models first.
2. Add persistence changes with migration-safe SQL.
3. Keep endpoints deterministic and JSON-structured.
4. Run `python3 -m compileall backend/app`.
